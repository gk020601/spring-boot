apiVersion: v1
kind: Service
metadata:
  name: springboot-app-nodeport
  namespace: koteswararao-namespace
spec:
  type: NodePort
  selector:
    app: springboot-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007
